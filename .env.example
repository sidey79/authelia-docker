TZ=Europe/Berlin
BASE_STACK_DATA_PATH=/opt/docker/authelia
AUTHELIA_UID=2000
AUTHELIA_GID=2000

POSTGRES_DB=authelia
POSTGRES_USER=authelia

# Secrets are file-based via bind mount:
# ${BASE_STACK_DATA_PATH}/secrets -> /run/authelia-secrets (read-only in containers)
# Required files:
# - reset_password_jwt_secret
# - session_secret
# - storage_encryption_key
# - postgres_password
# - smtp_password (optional, if SMTP relay requires auth)

# SMTP relay integration (https://github.com/sidey79/smtp-relay-docker)
AUTHELIA_NOTIFIER_SMTP_ADDRESS=submission://smtp-relay:25
AUTHELIA_NOTIFIER_SMTP_SENDER=noreply@example.com
# Optional (only if SMTP auth is required by relay):
# AUTHELIA_NOTIFIER_SMTP_PASSWORD_FILE=/run/authelia-secrets/smtp_password
